<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".LoginActivity" >

    <TextView android:id="@+id/login_TextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/LogInActivity_login_TextView" />
    
    <EditText android:id="@+id/login_EditText"
        android:layout_width="@dimen/barcode_EditText_width"
        android:layout_height="wrap_content"
        android:layout_below="@id/login_TextView"
        android:inputType="number" />
    
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/login_EditText"
        android:text="@string/LogInActivity_login_Button"
        android:onClick="login" />

</RelativeLayout>
